services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: community-backend:local
    container_name: community-backend-local
    ports:
      - "8080:8080"
    env_file:
      - .env.local
    restart: unless-stopped